> # 🚀 Day 02 – Linux Basics & Command Line Fundamentals
>
> ## 🎯 Objective
> Build a strong foundation in **Linux command-line usage**, file system navigation, and basic file operations — essential skills for every DevOps engineer.
>
> ---
>
> ## 🐧 Why Linux for DevOps?
> Linux is the backbone of most:
> - Cloud platforms
> - CI/CD systems
> - Containers & Kubernetes
> - Production servers
>
> Mastering Linux basics is **non-negotiable** for DevOps.
>
> ---
>
> ## 📂 Linux File System Overview
> Key directories:
> - `/` → Root directory
> - `/etc` → Configuration files
> - `/var` → Logs and variable data
> - `/home` → User home directories
> - `/usr` → User binaries and libraries
> - `/tmp` → Temporary files
>
> ---
>
> ## 🛠️ Hands-on Commands Used
>
> ### ✅ Check Current Directory
> ```bash
> pwd
> ```
>
> ---
>
> ### ✅ List Files and Directories
> ```bash
> ls
> ls -l
> ls -a
> ```
>
> ---
>
> ### ✅ Navigate Between Directories
> ```bash
> cd /etc
> cd ..
> cd ~
> ```
>
> ---
>
> ### ✅ Create Files and Directories
> ```bash
> mkdir devops
> touch file1.txt
> ```
>
> ---
>
> ### ✅ View File Content
> ```bash
> cat /etc/passwd
> less /etc/passwd
> head -n 5 /etc/passwd
> tail -n 5 /etc/passwd
> ```
>
> ---
>
> ### ✅ Copy, Move, and Delete Files
> ```bash
> cp file1.txt file2.txt
> mv file2.txt /tmp/
> rm file1.txt
> ```
>
> ---
>
> ### ✅ Check File Permissions
> ```bash
> ls -l
> ```
>
> ---
>
> ## 📌 Real-World Use Cases
> - Navigating production servers
> - Inspecting configuration files
> - Verifying logs and system state
> - Troubleshooting deployment issues
>
> ---
>
> ## 🧠 Key Learnings
> - Linux CLI is the foundation of DevOps work
> - Understanding file systems improves troubleshooting
> - Simple commands are powerful when combined
> - Daily Linux usage builds confidence and speed
